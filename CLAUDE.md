# CLAUDE.md

Repo-specific configuration the engineering skills expect. Generated by `/setup-skills` — edit `docs/agents/*.md` directly to adjust.

## Agent skills

### Ticket tracker

Tickets and PRDs live as GitHub issues in this repo, managed via the `gh` CLI. See `docs/agents/ticket-tracker.md`.

### Triage labels

Triage uses the canonical role names as label strings (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
